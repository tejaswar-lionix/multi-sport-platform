version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: sport
      POSTGRES_USER: sport
      POSTGRES_PASSWORD: sport
    ports: ["5432:5432"]
  web:
    build: .
    ports: ["8000:8000"]
    volumes: [".:/app"]
