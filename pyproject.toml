[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "multi-sport-platform"
version = "1.0.0"
description = "Multi-Sport Platform"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Proprietary"}
